<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.ruoxue.miyukisyllabus">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="org.ruoxue.miyukisyllabus.MainActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.SettingActivity"
            android:label="@string/title_activity_setting" />
        <activity
            android:name=".Activities.AboutActivity"
            android:label="@string/title_activity_about" />
        <activity
            android:name=".Activities.SyllabusActivity"
            android:label="@string/title_activity_syllabus" />
        <activity
            android:name=".Activities.ImportActivity"
            android:label="@string/title_activity_import" />
        <activity
            android:name=".Activities.GradePrepareActivity"
            android:label="@string/title_activity_grade_prepare" />
        <activity
            android:name=".Activities.GradeActivity"
            android:label="@string/title_activity_grade" />
        <activity
            android:name=".Activities.WebBrowserActivity"
            android:label="@string/title_activity_web_browser" />

        <service
            android:name=".Services.NotificationService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".Activities.EvalActivity"></activity>
    </application>

</manifest>